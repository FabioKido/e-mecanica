import React from 'react';

import {
  Wrapper,
  Container,
  Header,
  Title,
  ButtonText,
  FormButton,
  HelpText
} from './styles';

export default function Step2({ navigation }) {
  return(
    <Title>Olá</Title>
  );
}
